Router.configure({
    layoutTemplate: 'layout',
});


Router.route('/', function () {
  this.render('createTeams');
});

Router.rout('/rank-players', function () {
    this.render('rankPlayers');
});
